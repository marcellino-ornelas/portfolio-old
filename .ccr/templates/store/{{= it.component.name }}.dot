import { observable, computed, action, runInAction } from 'mobx';
import axios from 'axios';

export default class {{= it.component.name }}Store {
  @observable _ = _
	
	@action 
	_ () {}
	
	@computed 
	get _ () {}
	
}
